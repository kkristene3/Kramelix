# How to Run Test Scripts:

**REQUIRED**: Python 3.10.xx & all setup in SETUP.md.

In the root folder (`Kramelix\Kramelix_app\emotion_training`), run: `python -m tests.[script_name]` (e.g. `python -m tests.test_sample_data`)

_NOTE: (so far) only `test_sample_data.py` requires actual test data._
